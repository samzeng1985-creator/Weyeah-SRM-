# 合同审批流程实现总结

**实现日期：2026-05-21**
**版本：v1.1.1**

---

## ✅ 已实现内容

### 1. 后端实现（ContractController.java）

#### 1.1 添加金额等级常量
```java
private static final BigDecimal AMOUNT_LEVEL1 = new BigDecimal("50000");
private static final BigDecimal AMOUNT_LEVEL2 = new BigDecimal("200000");
private static final BigDecimal AMOUNT_LEVEL3 = new BigDecimal("1000000");
```

#### 1.2 修改提交审批方法
- 返回审批等级信息
- 提交后将状态改为 PENDING

#### 1.3 新增分级审批方法
- **采购经理审批** (`/api/contracts/{id}/approve`)
  - LEVEL1 (≤5万)：直接批准
  - LEVEL2 (5-20万)：提交财务审核
  - LEVEL3 (>20万)：提交财务审核

- **财务审核** (`/api/contracts/{id}/finance-approve`)
  - LEVEL2：直接批准
  - LEVEL3：提交法务审核

- **法务审核** (`/api/contracts/{id}/legal-approve`)
  - LEVEL3：提交总监审批
  - 其他：直接批准

- **采购总监审批** (`/api/contracts/{id}/director-approve`)
  - 最终审批

#### 1.4 修改驳回方法
- 支持所有审批状态（DRAFT/PENDING/FINANCE_PENDING/LEGAL_PENDING/DIRECTOR_PENDING）
- 驳回后状态改为草稿（DRAFT）

#### 1.5 添加辅助方法
- `getApprovalLevel(Contract)`：根据合同金额计算审批等级

### 2. 前端实现

#### 2.1 API调用（contract.ts）
新增API方法：
```typescript
- financeApprove(id): 财务审核
- legalApprove(id): 法务审核
- directorApprove(id): 总监审批
```

#### 2.2 页面操作按钮（Contracts.tsx）

**状态文本映射**：
- PENDING → "待采购审批"
- FINANCE_PENDING → "待财务审核"
- LEGAL_PENDING → "待法务审核"
- DIRECTOR_PENDING → "待总监审批"

**操作按钮逻辑**：
- DRAFT：提交审批
- PENDING：采购审批、驳回
- FINANCE_PENDING：财务审核、驳回
- LEGAL_PENDING：法务审核、驳回
- DIRECTOR_PENDING：总监审批、驳回
- APPROVED：签署完成
- SIGNED：合同生效
- ACTIVE：开始执行
- EXECUTING：完成合同

---

## 📊 合同分级审批规则

| 金额范围 | 审批流程 |
|---------|---------|
| ≤ 5万元 | 采购经理审批 → 批准 → 签署 → 生效 |
| 5-20万元 | 采购经理 → 财务审核 → 批准 → 签署 → 生效 |
| > 20万元 | 采购经理 → 财务审核 → 法务审核 → 总监审批 → 批准 → 签署 → 生效 |

---

## 🔄 审批状态流转

```
草稿(DRAFT)
    ↓ [提交审批]
待采购审批(PENDING)
    ↓ [采购经理审批]
    ├─→ 批准 → 待财务审核(FINANCE_PENDING) [金额>5万]
    │         ↓ [财务审核]
    │         ├─→ 批准 → 待法务审核(LEGAL_PENDING) [金额>20万]
    │         │         ↓ [法务审核]
    │         │         └─→ 待总监审批(DIRECTOR_PENDING)
    │         │                 ↓ [总监审批]
    │         └─→ 已批准(APPROVED)
    │
    └─→ 直接批准 → 已批准(APPROVED) [金额≤5万]

已批准(APPROVED)
    ↓ [签署完成]
已签署(SIGNED)
    ↓ [合同生效]
已生效(ACTIVE)
    ↓ [开始执行]
执行中(EXECUTING)
    ↓ [完成合同]
已完成(COMPLETED)
```

---

## 🎯 审批状态说明

| 状态码 | 状态名称 | 说明 |
|--------|---------|------|
| DRAFT | 草稿 | 合同创建中，可编辑 |
| PENDING | 待采购审批 | 等待采购经理审批 |
| FINANCE_PENDING | 待财务审核 | 等待财务审核 |
| LEGAL_PENDING | 待法务审核 | 等待法务审核（仅大额） |
| DIRECTOR_PENDING | 待总监审批 | 等待采购总监审批（仅大额） |
| APPROVED | 已批准 | 审批通过，待签署 |
| REJECTED | 已拒绝 | 审批被驳回，返回草稿 |
| SIGNED | 已签署 | 双方签署完成 |
| ACTIVE | 已生效 | 合同生效 |
| EXECUTING | 执行中 | 合同正在履行 |
| COMPLETED | 已完成 | 合同履行完毕 |
| EXPIRED | 已过期 | 到达到期日期 |
| TERMINATED | 已终止 | 提前终止 |

---

## 📁 修改的文件

### 后端
- `srm/srm-gateway/src/main/java/com/srm/gateway/controller/ContractController.java`
  - 添加金额等级常量
  - 修改提交审批方法
  - 添加采购经理审批方法（支持分级）
  - 添加财务审核方法
  - 添加法务审核方法
  - 添加采购总监审批方法
  - 修改驳回方法
  - 添加getApprovalLevel辅助方法

### 前端
- `srm-frontend-app/src/services/contract.ts`
  - 添加financeApprove API
  - 添加legalApprove API
  - 添加directorApprove API

- `srm-frontend-app/src/pages/Contracts.tsx`
  - 更新状态文本映射
  - 更新状态样式映射
  - 修改操作按钮逻辑（添加分级审批按钮）
  - 添加handleStatusChange中的新操作处理

---

## ✅ 测试要点

1. 创建合同（DRAFT状态）
2. 提交审批（→ PENDING）
3. 根据金额测试不同审批路径：
   - 小额（≤5万）：采购审批 → 批准
   - 中额（5-20万）：采购审批 → 财务审核 → 批准
   - 大额（>20万）：采购审批 → 财务审核 → 法务审核 → 总监审批 → 批准
4. 测试驳回功能（所有审批状态都可驳回）
5. 测试后续流程：签署 → 生效 → 执行 → 完成

---

**实现完成！合同审批流程与定价审批流程保持一致的设计模式。**
