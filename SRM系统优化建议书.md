# 燃气发电机零部件采购管理系统 - 方案优化建议书

## 0. 总体评估
本方案（PRD v1.4）业务闭环完整，针对**外贸零部件贸易**行业的“三件”管理（原装、OEM、替代件）及双仓库逻辑设计精准。系统通过飞书（内控审批）与企业微信（外联协同）的双集成，构建了良好的业务数字化基础。

为进一步提升系统的鲁棒性与合规性，建议在实施过程中重点关注以下优化方向：

---

## 1. 业务逻辑优化：深耕“物料-供应商-定价”模型

* **引入阶梯定价逻辑**：
    贸易场景中常存在“量大价优”，建议在定价管理增加阶梯报价（例如：1-10件 $X$ 元，11-50件 $Y$ 元），而非单一的最小起订量（MOQ）。
* **替代件成本拆解（核价模型）**：
    由于公司委托代加工自有品牌零部件，建议在委托加工合同中增加“材料费+加工费”的拆解项，以便在原材料（如钢材）波动时，系统能基于核价模型自动预警或计算调价补偿。
* **ERP 同步缓冲带**：
    主数据优先从 ERP 同步，但建议增加一个**数据中间层**进行清洗映射，防止 ERP 中历史沉淀的非规范数据（如旧编码）直接污染 SRM 的品类树。

---

## 2. 技术实现优化：合同模板与 PDF 渲染

* **CSS 媒体查询优化**：
    使用 HTML 转 PDF 易出现跨页断行、盖章位置错位问题。建议在模板管理中预设 **CSS 打印媒体查询（@media print）** 规范，强制定义分页符及合同章页边距。
* **电子签章预留锚点**：
    虽然目前支持手动上传扫描件，但建议在 HTML 模板中预留**电子签章 API 锚点**，为未来对接第三方电子合同平台（如契约锁、法大大）做准备。
* **多语言变量解耦**：
    建议将 HTML 模板中的静态文字与动态变量彻底解耦。采用 I18n 资源包模式，模板中仅存放 Key，方便行政人员在不修改代码的情况下修正合同法律术语。

---

## 3. 集成策略优化：身份对齐与流程动态化

* **统一身份账号中心 (UAC)**：
    供应商在企业微信中可能是“外部联系人”，建议在系统底层建立 `Supplier_ID` 与 `WeCom_UserID` 的**永久映射池**。即使供应商更换业务员，历史订单的权限也能平滑过渡，避免数据孤岛。
* **审批流引擎动态化**：
    建议实现**可视化审批引擎**，让采购经理能自主调整各金额段对应的审批人，而非硬编码在程序中。
* **飞书集成建议**：
    强烈建议优先采用 **“飞书审批 API”** 对接，而非多维表格。API 模式在处理合同明细子表（多物品采购）的权限控制和审计追踪方面更具优势。

---

## 4. 安全与合规优化：图纸保密控制

针对自有品牌替代件的图纸管理，需强化知识产权保护：

* **动态水印预览**：
    供应商在企微端查看图纸时，系统必须动态叠加包含“供应商全称 + 查看时间 + 登录 IP”的半透明水印。
* **下载及转发控制**：
    建议在企微 H5 容器中禁用长按保存及浏览器下载功能，核心图纸应通过特定的 **PDF 预览组件** 渲染，而非直接传输原始文件。
* **审计日志增强**：
    需详细记录供应商对每一份图纸的点击、停留时长、预览操作，作为 NDA 协议履约过程中的电子证据储备。

---

## 5. 实施路径优化：缩短“价值感知”周期

目前的 10 个月周期较长，建议调整如下：

* **供应商门户（Portal）前置**：
    将“供应商在线注册、资质上传及审核”提前至 Phase 1。先让供应商跑起来，将数据录入压力转嫁给外部，采购人员负责审核即可。
* **分步集成策略**：
    无需等待所有 ERP 接口调通。可先通过 Excel 完成存量物料导入，立即进入核心业务试运行，再并行开发增量同步接口。

---

**专家点评**：
> 本方案在业务架构层面已非常成熟。通过上述关于**身份池映射、合同渲染合规、图纸安全管控**的优化，能有效降低项目在中后期实施时的返工风险，并显著提升企业对核心技术资产（图纸）的掌控力。
