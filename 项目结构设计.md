# 项目结构设计文档

## 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 燃气发电机零部件采购管理系统 |
| 文档类型 | 项目结构设计 |
| 创建日期 | 2026-05-12 |
| 架构风格 | 多模块分层架构（预留微服务扩展） |

---

## 1. 架构选型说明

### 1.1 推荐方案：单应用多模块（Monolith + Multi-module）

**原因：**
- ✅ 初期简单，快速上线
- ✅ 部署运维简单
- ✅ 事务一致性好处理
- ✅ 预留接口，未来可按需拆成微服务

**进阶方案：** 未来可按需拆分为微服务

---

## 2. 完整项目结构

### 2.1 目录树结构

```
srm-system/
│
├── srm-parent/                          # 父项目（POM管理）
│   └── pom.xml                          # 全局依赖管理
│
├── srm-framework/                       # 框架层（所有模块依赖）
│   ├── framework-common/                # 通用基础模块
│   │   ├── src/main/java
│   │   │   ├── com.srm.common
│   │   │   │   ├── constant/           # 常量定义
│   │   │   │   ├── enums/              # 枚举
│   │   │   │   ├── exception/          # 异常类
│   │   │   │   ├── po/                 # 基础对象
│   │   │   │   ├── result/             # 统一响应
│   │   │   │   └── util/               # 工具类
│   │   └── pom.xml
│   │
│   ├── framework-web/                   # Web层框架
│   │   ├── src/main/java
│   │   │   └── com.srm.web
│   │   │       ├── config/             # Web配置
│   │   │       ├── handler/            # 全局异常处理
│   │   │       └── interceptor/        # 拦截器
│   │   └── pom.xml
│   │
│   ├── framework-data/                  # 数据层框架
│   │   ├── src/main/java
│   │   │   └── com.srm.data
│   │   │       ├── config/             # 数据源配置
│   │   │       ├── mybatis/            # MyBatis扩展
│   │   │       └── cache/              # 缓存配置
│   │   └── pom.xml
│   │
│   ├── framework-security/              # 安全认证框架
│   │   ├── src/main/java
│   │   │   └── com.srm.security
│   │   │       ├── authentication/     # 认证
│   │   │       ├── authorization/      # 授权
│   │   │       └── user/               # 用户信息
│   │   └── pom.xml
│   │
│   └── framework-integration/           # 第三方集成基础
│       ├── src/main/java
│       │   └── com.srm.integration
│       │       ├── feishu/             # 飞书集成基础
│       │       ├── wecom/              # 企业微信集成基础
│       │       └── erp/                # ERP集成基础
│       └── pom.xml
│
├── srm-modules/                        # 业务模块层
│   ├── srm-system/                     # 【模块1】组织架构和人员管理
│   │   ├── src/main/java
│   │   │   └── com.srm.system
│   │   │       ├── controller/         # Controller层
│   │   │       ├── service/            # Service层
│   │   │       │   ├── impl/           # Service实现
│   │   │       ├── dal/                # 数据访问层
│   │   │       │   ├── mysql/          # MySQL访问
│   │   │       │   │   ├── mapper/     # MyBatis Mapper
│   │   │       │   │   └── dataobject/ # DO对象
│   │   │       ├── vo/                 # 请求/响应VO
│   │   │       ├── convert/            # 对象转换
│   │   │       └── enums/              # 枚举
│   │   ├── src/main/resources
│   │   │   ├── mapper/                 # MyBatis XML
│   │   │   └── application.yml
│   │   ├── src/test/java
│   │   └── pom.xml
│   │
│   ├── srm-supplier/                   # 【模块2】供应商管理
│   │   ├── src/main/java
│   │   │   └── com.srm.supplier
│   │   │       ├── controller/         # API控制器
│   │   │       ├── service/            # 业务服务
│   │   │       ├── dal/                # 数据访问
│   │   │       ├── vo/                 # 接口VO
│   │   │       └── convert/            # 对象转换
│   │   ├── src/test/java
│   │   └── pom.xml
│   │
│   ├── srm-material/                   # 【模块3】物料和品类管理
│   │   ├── src/main/java
│   │   │   └── com.srm.material
│   │   │       ├── controller/         # 物料API
│   │   │       ├── service/            # 物料服务
│   │   │       ├── dal/                # 物料数据访问
│   │   │       ├── vo/                 # 物料VO
│   │   │       └── convert/            # 转换
│   │   ├── src/test/java
│   │   └── pom.xml
│   │
│   ├── srm-pricing/                    # 【模块4】定价管理
│   │   ├── src/main/java
│   │   │   └── com.srm.pricing
│   │   │       ├── controller/
│   │   │       ├── service/
│   │   │       ├── dal/
│   │   │       ├── vo/
│   │   │       └── convert/
│   │   └── pom.xml
│   │
│   ├── srm-contract/                   # 【模块5】合同管理（含模板）
│   │   ├── src/main/java
│   │   │   └── com.srm.contract
│   │   │       ├── controller/
│   │   │       ├── service/
│   │   │       │   ├── template/       # 模板服务
│   │   │       │   ├── pdf/            # PDF生成服务
│   │   │       ├── dal/
│   │   │       │   └── contract-template/ # 模板数据访问
│   │   │       ├── vo/
│   │   │       ├── convert/
│   │   │       └── engine/             # PDF渲染引擎
│   │   └── pom.xml
│   │
│   └── srm-integration/                # 【模块6】第三方集成
│       ├── srm-integration-erp/        # ERP集成子模块
│       │   ├── src/main/java
│       │   └── pom.xml
│       │
│       ├── srm-integration-feishu/     # 飞书集成子模块
│       │   ├── src/main/java
│       │   └── pom.xml
│       │
│       └── srm-integration-wecom/      # 企业微信集成子模块
│           ├── src/main/java
│           └── pom.xml
│
├── srm-gateway/                        # 网关和应用启动层
│   ├── srm-api-gateway/                # API网关（也可不用，先直接启动srm-server）
│   │   └── pom.xml
│   │
│   └── srm-server/                     # 应用启动入口
│       ├── src/main/java
│       │   └── com.srm.server
│       │       └── SrmApplication.java # Spring Boot启动类
│       ├── src/main/resources
│       │   ├── application.yml         # 主配置文件
│       │   ├── application-dev.yml     # 开发环境
│       │   ├── application-test.yml    # 测试环境
│       │   └── application-prod.yml    # 生产环境
│       └── pom.xml
│
├── srm-frontend/                       # 前端项目（独立仓库也可）
│   ├── src/
│   │   ├── api/
│   │   ├── components/
│   │   ├── pages/
│   │   ├── stores/
│   │   └── ...
│   └── package.json
│
├── sql/                                # 数据库脚本
│   ├── schema.sql                      # 建表语句
│   ├── data.sql                        # 初始化数据
│   └── migration/                      # 迁移脚本
│
├── docs/                               # 文档目录
│   ├── PRD.md
│   ├── 技术架构方案.md
│   ├── 开发规范.md
│   ├── 实施路径规划.md
│   └── ...
│
└── deploy/                             # 部署相关
    ├── docker-compose.yml
    ├── Dockerfile
    └── k8s/
```

---

## 3. 模块详细说明

### 3.1 模块清单及职责

| 模块名 | 层级 | 职责说明 | 依赖模块 |
|--------|------|---------|---------|
| **srm-parent** | 父项目 | 依赖版本管理、插件配置 | 无 |
| **srm-framework** | 框架层 | 提供通用框架能力 | 无 |
| **srm-system** | 业务 | 组织架构、用户、角色、权限 | framework |
| **srm-supplier** | 业务 | 供应商档案、资质、评估、标签 | framework, system |
| **srm-material** | 业务 | 物料、品类、图纸、扩展属性 | framework, system |
| **srm-pricing** | 业务 | 定价单、审批、变更、历史 | framework, system, supplier, material |
| **srm-contract** | 业务 | 合同创建、审批、PDF、模板 | framework, system, supplier, material, pricing |
| **srm-integration-erp** | 集成 | ERP同步、数据映射 | framework |
| **srm-integration-feishu** | 集成 | 飞书审批集成 | framework, system, contract |
| **srm-integration-wecom** | 集成 | 企业微信供应商协同 | framework, system, supplier, contract |
| **srm-server** | 启动 | 应用启动入口，集成所有模块 | 所有业务模块 |

### 3.2 模块间依赖关系

```
srm-server (启动层)
│
├─→ srm-system (基础)
├─→ srm-supplier (供应商)
├─→ srm-material (物料)
├─→ srm-pricing (定价)
│   ├─→ srm-supplier
│   └─→ srm-material
├─→ srm-contract (合同)
│   ├─→ srm-supplier
│   ├─→ srm-material
│   └─→ srm-pricing
└─→ srm-integration-xxx (集成)
    ├─→ srm-contract
    ├─→ srm-supplier
    └─→ srm-material

所有模块都依赖：srm-framework
```

### 3.3 模块依赖图

```
                 ┌─────────────────┐
                 │ srm-server      │  (启动入口)
                 └────────┬────────┘
                          │
        ┌─────────────────┼─────────────────┐
        │                 │                 │
        ▼                 ▼                 ▼
┌──────────────┐ ┌──────────────┐ ┌──────────────┐
│ srm-system   │ │srm-supplier  │ │srm-material  │
│ (基础)       │ │ (供应商)     │ │ (物料)       │
└──────────────┘ └───────┬──────┘ └───────┬──────┘
                         │                │
                         └────────┬───────┘
                                  │
                                  ▼
                         ┌───────────────┐
                         │ srm-pricing   │
                         │ (定价)        │
                         └───────┬───────┘
                                 │
                                 ▼
                         ┌───────────────┐
                         │ srm-contract  │
                         │ (合同)        │
                         └───────┬───────┘
                                 │
        ┌────────────────────────┼────────────────────────┐
        │                        │                        │
        ▼                        ▼                        ▼
┌───────────────┐    ┌───────────────┐    ┌───────────────┐
│  ERP 集成     │    │ 飞书集成      │    │ 企业微信集成   │
└───────────────┘    └───────────────┘    └───────────────┘

┌───────────────────────────────────────────────────────────────┐
│              srm-framework (框架层 - 所有模块依赖)             │
└───────────────────────────────────────────────────────────────┘
```

---

## 4. 模块独立性设计

### 4.1 各模块独立包

```
srm-system 的包：
com.srm.system.*

srm-supplier 的包：
com.srm.supplier.*

srm-material 的包：
com.srm.material.*

以此类推...
```

### 4.2 模块间调用规范

**原则：** 只通过 Service 接口调用，不直接访问其他模块的 Mapper

```java
// ✅ 正确做法：通过 Service 调用
@Service
@RequiredArgsConstructor
public class SomeService {
    
    private final SupplierService supplierService; // 来自其他模块的 Service
    
    public void doSomething() {
        SupplierRespVO supplier = supplierService.getSupplier(id);
    }
}

// ❌ 错误做法：不直接访问其他模块的 Mapper
// 不要这样做！
@Autowired
private SupplierMapper supplierMapper;
```

---

## 5. 未来微服务扩展路径

### 5.1 当前阶段（单应用多模块）

```
所有模块打成一个 JAR/WAR 包
┌─────────────────────────────────────┐
│  srm-server (包含所有模块)           │
└─────────────────────────────────────┘
```

### 5.2 阶段2：按需拆分（假设供应商访问量大）

```
┌─────────────────────────────────────┐
│  srm-server (除供应商外的模块)       │
└───────────┬─────────────────────────┘
            │ 调用
            ▼
┌─────────────────────────────────────┐
│  srm-supplier-service (独立部署)     │
└─────────────────────────────────────┘
```

### 5.3 完整微服务架构（未来）

```
API Gateway
│
├─→ srm-system-service (用户组织)
├─→ srm-supplier-service (供应商)
├─→ srm-material-service (物料)
├─→ srm-pricing-service (定价)
├─→ srm-contract-service (合同)
└─→ srm-integration-xxx (集成模块)
```

---

## 6. 模块开发顺序

### 6.1 第一阶段（基础）
1. ✅ srm-framework（框架搭建）
2. ✅ srm-system（组织用户权限）

### 6.2 第二阶段（核心数据）
3. ✅ srm-supplier（供应商管理）
4. ✅ srm-material（物料管理）

### 6.3 第三阶段（核心业务）
5. ✅ srm-pricing（定价管理）
6. ✅ srm-contract（合同管理）

### 6.4 第四阶段（集成）
7. ✅ srm-integration-xxx（第三方集成）

---

## 7. 关键设计原则

### 7.1 高内聚低耦合

| 原则 | 说明 |
|------|------|
| **高内聚** | 一个模块只做一件事，内部功能紧密相关 |
| **低耦合** | 模块间依赖最小化，通过接口调用 |

### 7.2 开闭原则

- 对扩展开放：可以通过添加新模块来扩展功能
- 对修改关闭：已有模块尽量少修改

### 7.3 依赖倒置原则

- 高层不依赖底层，都依赖抽象（接口）

---

## 8. 配套文档索引

| 文档 | 说明 | 链接 |
|------|------|------|
| 产品需求文档 | PRD | [PRD.md](file:///c:/Users/konst/Documents/Trae%20SOLO/Weyeah-SRM/PRD.md) |
| 技术架构方案 | 系统架构设计 | [技术架构方案.md](file:///c:/Users/konst/Documents/Trae%20SOLO/Weyeah-SRM/技术架构方案.md) |
| 开发规范 | 代码规范 | [开发规范.md](file:///c:/Users/konst/Documents/Trae%20SOLO/Weyeah-SRM/开发规范.md) |
| 实施路径规划 | 项目计划 | [实施路径规划.md](file:///c:/Users/konst/Documents/Trae%20SOLO/Weyeah-SRM/实施路径规划.md) |

---

**文档结束**
