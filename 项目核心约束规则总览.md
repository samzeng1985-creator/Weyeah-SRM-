# 项目核心约束规则总览

## 文档信息

| 项目 | 内容 |
|------|------|
| 文档名称 | 项目核心约束规则总览 |
| 创建日期 | 2026-05-12 |
| 说明 | 涵盖前后端开发、代码管理、部署运维的全部核心约束 |
| 配套文档 | [前端库依赖关系与约束规则.md](file:///c:/Users/konst/Documents/Trae%20SOLO/Weyeah-SRM/前端库依赖关系与约束规则.md) |

---

## 1. 约束规则总览

我们已将约束规则分为 6 大类别：

| 类别 | 规则数量 | 说明 |
|------|---------|------|
| **1. 代码结构约束** | 8条 | 包/目录/命名规范 |
| **2. 前后端交互约束** | 6条 | API设计/数据格式 |
| **3. 安全性约束** | 7条 | 认证/授权/数据安全 |
| **4. 性能约束** | 5条 | 响应时间/资源限制 |
| **5. 质量约束** | 6条 | 测试/代码审查/文档 |
| **6. 部署运维约束** | 5条 | 环境/配置/监控 |

---

## 2. 前端开发规范约束（补充）

### 2.1 文件命名约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **FE-01** | React 组件文件必须使用 PascalCase.tsx 命名 | ESLint 错误 |
| **FE-02** | 非组件文件（工具函数、类型定义）必须使用 kebab-case.ts 命名 | ESLint 错误 |
| **FE-03** | 同一功能模块的组件和文件使用统一前缀 | Code Review 拒绝 |
| **FE-04** | 测试文件必须与源文件同名，后缀 .test.ts(x) | CI 构建警告 |
| **FE-05** | 样式文件必须与对应组件同名，后缀 .module.css/.module.scss | ESLint 警告 |
| **FE-06** | 禁止使用无意义或中文拼音命名 | ESLint 错误 |

```
文件命名示例:
────────────────────────────────────
✅ components/
   ├── UserProfile.tsx          (组件)
   ├── user-profile.ts          (工具函数)
   ├── user-profile.module.css  (样式)
   ├── use-user-profile.ts      (Hook)
   └── user-profile.test.tsx    (测试)

❌ components/
   ├── userprofile.tsx
   ├── UserProfile.ts
   ├── yonghu.tsx
```

### 2.2 包引用约束（Monorepo）

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **FE-07** | Monorepo 内部包引用必须使用 @srm/ 前缀 | TypeScript 错误 |
| **FE-08** | 禁止跨包直接引用内部实现细节 | ESLint 错误 |
| **FE-09** | 第三方依赖版本必须锁定，禁止使用 latest | CI 构建警告 |
| **FE-10** | 禁止引入重复依赖（peerDependencies 除外） | CI 构建警告 |

```
包引用示例:
────────────────────────────────────
✅ 正确引用:
import { Button } from '@srm/ui';
import { useAuth } from '@srm/auth';
import { formatDate } from '@srm/utils';

❌ 错误引用:
import { Button } from '@/components/ui/button';
import { useAuth } from '../../auth/hooks/useAuth';
```

### 2.3 导入顺序约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **FE-11** | 导入顺序必须为：外部库 → 内部包(@srm/) → 相对路径 | ESLint 错误 |
| **FE-12** | 同组导入按字母顺序排列 | ESLint 警告 |
| **FE-13** | 各组导入之间用空行分隔 | ESLint 错误 |

```
导入顺序示例:
────────────────────────────────────
✅ 正确示例:
import React, { useState, useEffect } from 'react';
import { useNavigate } from 'react-router-dom';
import { message } from 'antd';

import { Button } from '@srm/ui';
import { useAuth } from '@srm/auth';
import { formatDate } from '@srm/utils';

import { UserCard } from './components/UserCard';
import { useUserList } from './hooks/useUserList';
import type { User } from './types';
import './styles/user-list.css';

❌ 错误示例:
import { UserCard } from './components/UserCard';
import React from 'react';
import { useAuth } from '@srm/auth';
```

### 2.4 组件与 Hooks 命名约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **FE-14** | React 组件必须使用 PascalCase | TypeScript 错误 |
| **FE-15** | 自定义 Hooks 必须使用 use 前缀 | ESLint 警告 |
| **FE-16** | 事件处理函数必须使用 handle 前缀 | ESLint 警告 |
| **FE-17** | 布尔值变量必须使用 is/has/should 前缀 | ESLint 警告 |
| **FE-18** | 组件内部状态使用 useState，返回值使用 [state, setState] 模式 | ESLint 警告 |

```
命名约束示例:
────────────────────────────────────
✅ 正确命名:
const UserProfile = () => { ... }
const useUserData = () => { ... }
const handleClick = () => { ... }
const isLoading = false;
const hasPermission = true;
const shouldUpdate = true;

❌ 错误命名:
const userProfile = () => { ... }
const UserData = () => { ... }
const onClick = () => { ... }
const loading = false;
const permission = true;
```

### 2.5 类型定义约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **FE-19** | 接口必须使用 I 前缀 | ESLint 警告 |
| **FE-20** | 类型别名必须使用 T 前缀 | ESLint 警告 |
| **FE-21** | 枚举必须使用 E 前缀 | ESLint 警告 |
| **FE-22** | 优先使用 interface 定义对象类型 | ESLint 提示 |
| **FE-23** | 必须导出所有公共类型，禁止使用 any | TypeScript 错误 |

```
类型定义示例:
────────────────────────────────────
✅ 正确示例:
interface IUser {
  id: string;
  name: string;
  email: string;
}

type TUserRole = 'admin' | 'user' | 'guest';

enum EUserStatus {
  Active = 'active',
  Inactive = 'inactive',
  Banned = 'banned',
}

type TUserResponse = {
  user: IUser;
  role: TUserStatus;
};

interface IProps {
  user: IUser;
  onUpdate: (user: IUser) => void;
}

❌ 错误示例:
interface User {
  id: string;
}
const userRole = 'admin';
enum Status {
  Active,
}
```

### 2.6 函数命名约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **FE-24** | 普通函数必须使用 camelCase | ESLint 错误 |
| **FE-25** | 纯函数优先，避免副作用 | ESLint 提示 |
| **FE-26** | 异步函数必须使用 async/await，禁止使用 .then().catch() 链式调用 | ESLint 警告 |
| **FE-27** | 导出函数必须有明确返回值类型注解 | TypeScript 警告 |
| **FE-28** | 禁止使用 arguments 对象，使用 rest 参数 | ESLint 错误 |

```
函数命名示例:
────────────────────────────────────
✅ 正确示例:
function calculateTotal(items: IItem[]): number {
  return items.reduce((sum, item) => sum + item.price, 0);
}

async function fetchUser(id: string): Promise<IUser> {
  const response = await api.get(`/users/${id}`);
  return response.data;
}

const formatCurrency = (amount: number): string => {
  return `¥${amount.toFixed(2)}`;
};

❌ 错误示例:
function Calculate_Total(items) { ... }
function fetchUser(id) {
  return api.get(`/users/${id}`).then(res => res.data);
}
```

### 2.7 常量命名约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **FE-29** | 常量必须使用 UPPER_SNAKE_CASE | ESLint 错误 |
| **FE-30** | 常量必须放在 constants/ 目录或文件顶部 | ESLint 警告 |
| **FE-31** | 禁止使用魔法数字，必须定义常量 | ESLint 错误 |
| **FE-32** | 相关常量必须使用 Object.freeze() 或 const 枚举 | ESLint 提示 |

```
常量命名示例:
────────────────────────────────────
✅ 正确示例:
constants/
├── api.ts
│   export const API_BASE_URL = 'https://api.example.com';
│   export const API_TIMEOUT = 30000;
│
├── pagination.ts
│   export const DEFAULT_PAGE_SIZE = 20;
│   export const MAX_PAGE_SIZE = 100;
│
└── user.ts
    export const USER_ROLES = Object.freeze({
      ADMIN: 'admin',
      USER: 'user',
      GUEST: 'guest',
    });

// 使用常量
const pageSize = DEFAULT_PAGE_SIZE;
if (user.role === USER_ROLES.ADMIN) { ... }

❌ 错误示例:
const pageSize = 20;
const timeout = 30000;
if (role === 'admin') { ... }
```

### 2.8 错误处理约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **FE-33** | 异步操作必须使用 try-catch 捕获错误 | ESLint 错误 |
| **FE-34** | 禁止静默失败，必须处理所有可能的错误 | Code Review 拒绝 |
| **FE-35** | 自定义错误类型必须继承 Error 或其子类 | TypeScript 警告 |
| **FE-36** | 错误信息必须包含足够的上下文信息 | Code Review 警告 |
| **FE-37** | 前端错误必须上报到监控系统 | Code Review 警告 |

```
错误处理示例:
────────────────────────────────────
✅ 正确示例:
class ApiError extends Error {
  constructor(
    message: string,
    public code: string,
    public statusCode: number
  ) {
    super(message);
    this.name = 'ApiError';
  }
}

async function fetchUserData(userId: string): Promise<IUser> {
  try {
    const response = await api.get(`/users/${userId}`);
    return response.data;
  } catch (error) {
    if (error instanceof ApiError) {
      console.error(`API Error: ${error.code}`, error);
      message.error(`获取用户失败: ${error.message}`);
    } else {
      console.error('Unknown Error:', error);
      message.error('网络错误，请稍后重试');
    }
    throw error;
  }
}

// 组件中使用
const handleSubmit = async () => {
  try {
    setLoading(true);
    await fetchUserData(userId);
    message.success('保存成功');
  } catch (error) {
    // 错误已在 fetchUserData 中处理，这里不需要再次处理
  } finally {
    setLoading(false);
  }
};

❌ 错误示例:
async function fetchUserData(userId: string) {
  const response = await api.get(`/users/${userId}`);
  return response.data;
}

// 静默失败
const handleSubmit = () => {
  fetchUserData(userId);
};
```

### 2.9 注释规范（JSDoc）

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **FE-38** | 导出函数必须使用 JSDoc 注释 | ESLint 警告 |
| **FE-39** | React 组件必须包含 Props 和返回值注释 | ESLint 警告 |
| **FE-40** | 复杂业务逻辑必须添加注释说明 | Code Review 拒绝 |
| **FE-41** | 注释必须使用中文，禁止中英混合 | ESLint 警告 |
| **FE-42** | TODO/FIXME 必须包含关联 Issue 编号 | ESLint 警告 |

```
JSDoc 注释示例:
────────────────────────────────────
✅ 正确示例:
/**
 * 格式化日期显示
 * @param date - 日期对象或时间戳
 * @param format - 格式化模板，默认为 'YYYY-MM-DD'
 * @returns 格式化后的日期字符串
 */
export function formatDate(date: Date | number, format: string = 'YYYY-MM-DD'): string {
  // 实现逻辑
}

/**
 * 用户卡片组件
 * @description 显示用户基本信息，支持编辑功能
 * @example
 * ```tsx
 * <UserCard
 *   user={{ id: '1', name: '张三', email: 'zhangsan@example.com' }}
 *   onEdit={(user) => handleEdit(user)}
 * />
 * ```
 */
interface IUserCardProps {
  user: IUser;
  onEdit: (user: IUser) => void;
  isLoading?: boolean;
}

export const UserCard: React.FC<IUserCardProps> = ({ user, onEdit, isLoading }) => {
  // 复杂逻辑注释
  // 由于后端返回的角色字段与其他模块不一致，这里需要做映射转换
  const mappedRole = ROLE_MAP[user.role] ?? 'unknown';
  
  return (
    <Card>
      {/* 状态显示注释 */}
      <Badge status={user.isActive ? 'success' : 'default'} />
    </Card>
  );
};

/**
 * TODO: 用户权限验证逻辑待优化
 * @see https://github.com/org/project/issues/1005
 */
function checkPermission() { ... }

❌ 错误示例:
// 格式化日期
export function formatDate(date, format = 'YYYY-MM-DD') {
  // ...
}

// 没有注释
const UserCard = ({ user, onEdit }) => { ... };
```

---

## 3. 代码结构约束

### 3.1 后端代码结构约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **SC-01** | 包名必须使用小写，多个单词用 `.` 分隔 | 编译错误 |
| **SC-02** | Controller 只做参数校验和调用 Service，禁止业务逻辑 | Code Review 拒绝 |
| **SC-03** | Service 只做业务逻辑，不直接操作数据库 | Code Review 拒绝 |
| **SC-04** | Repository 只做数据访问，禁止业务逻辑 | Code Review 拒绝 |
| **SC-05** | DO 对象只用于数据库映射，禁止用于参数接收 | Code Review 拒绝 |
| **SC-06** | VO 对象禁止包含业务方法，只做数据传输 | Code Review 拒绝 |
| **SC-07** | 所有异常必须使用自定义异常，禁止抛出 RuntimeException | Code Review 拒绝 |
| **SC-08** | 禁止在 Controller 中使用 try-catch，统一使用全局异常处理器 | Code Review 拒绝 |

### 2.2 前端代码结构约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **FC-01** | 组件文件必须使用 PascalCase 命名 | ESLint 错误 |
| **FC-02** | 工具函数必须使用 camelCase 命名 | ESLint 错误 |
| **FC-03** | 页面组件必须在 pages/ 目录下 | ESLint 错误 |
| **FC-04** | 禁止在模板中写复杂逻辑，必须使用计算属性或方法 | ESLint 警告 |
| **FC-05** | 所有 API 调用必须通过 api/ 目录，禁止在组件中直接调用 | Code Review 拒绝 |
| **FC-06** | 禁止在组件中直接使用 Element Plus，必须封装到 libs/components | Code Review 拒绝 |
| **FC-07** | 所有字符串必须使用 i18n，不允许硬编码中文 | ESLint 错误 |
| **FC-08** | 组件 Props 必须使用 TypeScript 类型定义 | TypeScript 错误 |

---

## 3. 前后端交互约束

### 3.1 API 设计约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **API-01** | 所有 API 必须使用 RESTful 风格 | API Review 拒绝 |
| **API-02** | GET 请求禁止有请求体，所有参数通过 Query Parameter 传递 | API Review 拒绝 |
| **API-03** | 所有 API 必须有 Swagger/OpenAPI 注解 | API Review 警告 |
| **API-04** | 分页查询必须使用统一分页格式 `{ page, pageSize, total, records }` | API Review 拒绝 |
| **API-05** | 列表查询必须支持分页，禁止一次返回全部数据 | API Review 拒绝 |
| **API-06** | 所有响应必须使用统一格式 `{ code, message, data }` | API Review 拒绝 |

### 3.2 数据格式约束

```
统一请求格式:
────────────────────────────────────
GET  /api/supplier/suppliers?page=1&pageSize=20&keyword=xxx
POST /api/supplier/suppliers
Content-Type: application/json

{
  "supplierName": "xxx",
  "supplierType": "domestic"
}

统一响应格式:
────────────────────────────────────
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "records": [...],
    "total": 100,
    "page": 1,
    "pageSize": 20
  }
}

错误响应:
────────────────────────────────────
{
  "code": 400,
  "message": "参数校验失败",
  "data": null
}
```

---

## 4. 安全性约束

### 4.1 认证授权约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **SEC-01** | 所有 API 必须认证，未认证返回 401 | 安全审计拒绝 |
| **SEC-02** | 敏感操作必须记录操作日志 | 安全审计拒绝 |
| **SEC-03** | 密码必须加密存储，禁止明文 | 安全审计拒绝 |
| **SEC-04** | Token 有效期不超过 2 小时 | 安全审计拒绝 |
| **SEC-05** | 禁止在 URL 中传递敏感参数 | 安全审计拒绝 |
| **SEC-06** | SQL 查询必须使用参数化查询，禁止拼接 SQL | 安全审计拒绝 |
| **SEC-07** | 外部输入必须校验和过滤，禁止 XSS | 安全审计拒绝 |

### 5.2 数据安全约束

```
敏感数据处理规则:
────────────────────────────────────

❌ 禁止:
- 在日志中打印密码、Token
- 在 URL 中传递 ID 不做权限校验
- 返回不必要的敏感字段
- 文件上传无大小限制

✅ 必须:
- 密码使用 BCrypt 加密
- Token 使用 JWT
- 敏感字段在返回前脱敏
- 文件上传限制大小（单文件 ≤ 10MB）
```

---

## 5. 性能约束

### 5.1 响应时间约束

| 规则编号 | 约束规则 | 阈值 | 违规处理 |
|---------|---------|------|---------|
| **PER-01** | 单个 API 响应时间 | ≤ 500ms | 性能警告 |
| **PER-02** | 复杂查询 API 响应时间 | ≤ 2s | 性能警告 |
| **PER-03** | PDF 导出单合同 | ≤ 5s | 性能警告 |
| **PER-04** | 页面首次加载 | ≤ 3s | 性能警告 |
| **PER-05** | 数据库单次查询 | ≤ 100ms | 性能警告 |

### 6.2 资源使用约束

```
资源使用规则:
────────────────────────────────────

后端:
- 单次请求内存使用 ≤ 256MB
- 单个文件上传大小 ≤ 10MB
- 单次批量操作 ≤ 100 条
- 数据库连接池 ≤ 50

前端:
- 单个 bundle ≤ 500KB
- 首屏加载资源 ≤ 2MB
- 单个组件 ≤ 50KB
- 图片懒加载，非首屏图片必须懒加载
```

---

## 6. 质量约束

### 6.1 测试约束

| 规则编号 | 约束规则 | 覆盖率要求 | 违规处理 |
|---------|---------|-----------|---------|
| **TEST-01** | 后端 Service 层单元测试 | ≥ 80% | CI 构建失败 |
| **TEST-02** | 后端 Controller 层单元测试 | ≥ 70% | CI 构建失败 |
| **TEST-03** | 核心业务逻辑测试 | 100% | CI 构建失败 |
| **TEST-04** | 前端组件测试 | ≥ 60% | CI 构建警告 |
| **TEST-05** | 集成测试（端到端） | 每个流程 ≥ 1 | CI 构建警告 |
| **TEST-06** | 所有新功能必须附带测试 | 100% | PR 拒绝合并 |

### 7.2 代码审查约束

```
代码审查规则:
────────────────────────────────────

必须审查:
✅ 所有 Pull Request 必须审查
✅ 至少 1 人批准才能合并
✅ 高风险变更（支付、安全）需 2 人批准

审查清单:
□ 逻辑正确性
□ 代码规范符合度
□ 测试覆盖度
□ 性能影响
□ 安全影响
□ 文档更新

禁止合并:
❌ 测试未通过
❌ 代码覆盖率下降
❌ ESLint/TSLint 报错
❌ 安全漏洞
❌ 循环依赖
```

### 6.3 文档约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **DOC-01** | 新增 API 必须更新接口文档 | PR 拒绝合并 |
| **DOC-02** | 数据库表变更必须更新 ER 图 | PR 拒绝合并 |
| **DOC-03** | 重要业务逻辑必须添加注释 | Code Review 拒绝 |
| **DOC-04** | README 必须包含运行说明 | PR 拒绝合并 |

---

## 7. 部署运维约束

### 7.1 环境约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **ENV-01** | 禁止在生产环境直接修改代码 | 违规追责 |
| **ENV-02** | 生产环境配置必须通过环境变量，禁止硬编码 | 安全审计拒绝 |
| **ENV-03** | 不同环境使用不同数据库 | 环境隔离 |
| **ENV-04** | 敏感配置（密码/密钥）必须加密存储 | 安全审计拒绝 |
| **ENV-05** | 上线前必须经过测试环境和预发布环境验证 | 发布流程拒绝 |

### 8.2 配置管理约束

```
配置管理规则:
────────────────────────────────────

配置文件优先级（从高到低）:
1. 环境变量 (最高)
2. 命令行参数
3. application-{profile}.yml
4. application.yml (最低)

禁止在代码中硬编码:
❌ 数据库密码
❌ API 密钥
❌ 第三方平台凭证
❌ 业务开关

必须通过环境变量:
✅ spring.datasource.password
✅ jwt.secret
✅ feishu.appSecret
✅ wecom.corpSecret
```

---

## 8. Git 管理约束

### 8.1 分支管理约束

| 规则编号 | 约束规则 | 违规处理 |
|---------|---------|---------|
| **GIT-01** | main 分支禁止直接推送，必须通过 PR | Git Hook 拒绝 |
| **GIT-02** | 功能开发必须从 develop 分支创建 | Git Hook 警告 |
| **GIT-03** | 分支命名必须符合规范 | Git Hook 警告 |
| **GIT-04** | Commit Message 必须符合规范 | Git Hook 警告 |
| **GIT-05** | 禁止在 main 分支进行开发 | Git Hook 拒绝 |

### 8.2 分支命名规范

```
分支命名规则:
────────────────────────────────────

格式: <type>/<issue-id>-<简短描述>

类型:
- feature/     新功能
- bugfix/       Bug修复
- hotfix/       紧急修复
- refactor/     重构
- docs/         文档更新
- test/         测试相关
- chore/        构建/工具

示例:
✅ feature/1001-supplier-management
✅ bugfix/1002-supplier-list-bug
✅ hotfix/1003-login-error
✅ refactor/1004-code-optimization
❌ new-feature
❌ fix-bug
❌ abc
```

### 8.3 Commit Message 规范

```
Commit Message 规范:
────────────────────────────────────

格式:
<type>(<scope>): <subject>

<body>

<footer>

示例:
feat(supplier): 添加供应商分类标签功能

- 支持添加多个标签
- 标签筛选功能
- 标签管理页面

Closes #1001
Refs #1002

类型说明:
- feat: 新功能
- fix: Bug修复
- docs: 文档更新
- style: 代码格式（不影响功能）
- refactor: 重构
- test: 测试相关
- chore: 构建/工具

规范检查:
✅ 必须有 type
✅ subject 不超过 50 字符
✅ body 详细说明修改内容
✅ 关联 Issue 或 PR
```

---

## 10. 速查约束表

### 10.1 严重违规（CI 失败/禁止合并）

| 违规项 | 对应规则 |
|--------|---------|
| 循环依赖 | SC / FC |
| SQL 注入风险 | SEC-06 |
| 敏感数据泄露 | SEC-01/03/05 |
| 测试覆盖率不达标 | TEST-01/02/03/06 |
| 代码规范检查失败 | SC / FC |
| main 分支直接推送 | GIT-01/05 |

### 10.2 警告项（Code Review 关注）

| 违规项 | 对应规则 |
|--------|---------|
| 代码结构不规范 | SC-02/03/04/05 |
| API 设计不规范 | API-01/02/03/04/05/06 |
| 性能不达标 | PER-01/02/03/04/05 |
| 缺少必要注释 | DOC-03 |
| 分支命名不规范 | GIT-02/03/04 |

---

## 10. 约束执行工具

### 10.1 后端检查工具

```bash
# 代码格式检查
mvn spotless:apply

# 代码规范检查
mvn checkstyle:check

# 单元测试
mvn test

# 集成测试
mvn verify

# 完整检查
mvn clean verify
```

### 11.2 前端检查工具

```bash
# ESLint 检查
npm run lint

# 自动修复
npm run lint:fix

# TypeScript 类型检查
npm run typecheck

# 单元测试
npm run test

# 完整检查
npm run build
```

### 11.3 Git Hooks 配置

```yaml
# .husky/pre-commit
npm run lint
npm run test

# .husky/commit-msg
npx commitlint --edit $1

# .husky/pre-push
npm run build
```

---

**文档结束**
